import React from 'react';
import TransmissionContainer from '../components/TransmissionContainer/TransmissionContainer';

const Transmission = () => {
    return (
        <TransmissionContainer></TransmissionContainer>
    );
};

export default Transmission;